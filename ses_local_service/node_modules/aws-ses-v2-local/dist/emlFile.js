"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createEmlContent = void 0;
const nodemailer_1 = require("nodemailer");
const store_1 = require("./store");
const createEmlContent = async (email) => {
    const transporter = (0, nodemailer_1.createTransport)({
        streamTransport: true,
        buffer: true,
    });
    return new Promise((resolve, reject) => {
        transporter.sendMail((0, store_1.convertToMailOptions)(email), (error, info) => {
            if (error) {
                reject(error);
            }
            else {
                resolve({ messageId: email.messageId, fileName: email.subject, body: info.message });
            }
        });
    });
};
exports.createEmlContent = createEmlContent;
